package funkin.chart;

import flixel.FlxG;
import funkin.game.objects.HealthIcon;
import funkin.scenes.PlayState;
import funkin.system.Song.BPMChangeEvent;
class Chart
{
    // This is the chart editor, will do this later or maybe when someone else will do it
}